# TO BUILD:
- (only runs on Linux/WSL)
- Install LibGMP ("sudo apt install libgmp-dev" on Debian-based systems)
- Install GCC/G++
- Install Make
- Run Makefile ("make")

# TO RUN:
- Install LibGMP ("sudo apt install libgmp-dev" on Debian-based systems)
- Run the program ("./main")
